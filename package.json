{
  "name": "zync-erp",
  "version": "1.0.0",
  "description": "ZYNC ERP - Integrated Retail & Wholesale Management System",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "test:backend": "npm test --prefix backend -- --runInBand --silent",
    "test:frontend": "npm test --prefix frontend -- --run",
    "test": "npm run test:backend && npm run test:frontend",
    "test:unit": "npm run test",
    "heroku-postbuild": "cd backend && npm install --production=false"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "erp",
    "pos",
    "react",
    "node",
    "postgres"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
